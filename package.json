{
  "name": "ci-server",
  "private": true,
  "scripts": {
    "run-config-server": "lerna run start --scope=@ci-server/config-server*",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
